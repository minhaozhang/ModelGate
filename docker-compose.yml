services:
  modelgate:
    build: .
    ports:
      - "8765:8765"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
