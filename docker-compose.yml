services:
  api-proxy:
    build: .
    ports:
      - "8765:8765"
    environment:
      - DATABASE_URL=postgresql+asyncpg://api-proxy:Zaq1%403edc@192.168.58.128/api-proxy
      - PORT=8765
      - ADMIN_PASSWORD=ZxcvbnmZaq1#)
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
